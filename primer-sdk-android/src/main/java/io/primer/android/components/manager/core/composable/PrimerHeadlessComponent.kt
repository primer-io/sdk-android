package io.primer.android.components.manager.core.composable

/**
 * An interface representing a headless component.
 * Headless components do not have a graphical user interface (GUI) and typically
 * perform background or non-visual tasks within the system.
 */
interface PrimerHeadlessComponent
