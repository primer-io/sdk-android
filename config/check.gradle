// TODO add dependency on lint task if there is one && add dependency on testing tasks

task("staticAnalysis") {
    group = "verification"
    afterEvaluate {
        def taskDependencies = ["ktlintCheck", "detekt"]
        taskDependencies.forEach({
            if (tasks.names.contains(it)) {
                dependsOn(it)
            }
        })
    }
}
